{
  "throws": "`y` is shadowed from a higher scope. If you want to reassign the variable, use `now y = ...`. If you want to declare a new shadowed `const` variable, you must use `const y = ...` explicitly."
}
